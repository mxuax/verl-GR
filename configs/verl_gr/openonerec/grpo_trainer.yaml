# OpenOneRec GRPO config for verl release/v0.7.1.
# Compose from upstream ppo_trainer to inherit all struct keys
# (notably global_profiler, ray_kwargs, transfer_queue) and dataclass targets.

hydra:
  searchpath:
    - pkg://verl.trainer.config

defaults:
  - ppo_trainer
  - _self_

# ---------- GRPO-specific overrides ----------

actor_rollout_ref:
  hybrid_engine: true
  rollout:
    layered_summon: false

algorithm:
  adv_estimator: grpo
  norm_adv_by_std_in_grpo: true
  use_kl_in_reward: false

trainer:
  project_name: OneRec_RL
  experiment_name: grpo_two_stage
  val_before_train: true

# ray_init is consumed by main_onerec_ppo.py (config.ray_init.num_cpus)
# before it delegates to main_ppo.run_ppo, which reads ray_kwargs instead.
ray_init:
  num_cpus: null
  timeline_json_file: null
