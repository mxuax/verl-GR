# Configs

This directory is reserved for task and stage configuration files.

Phase A only creates the directory boundary. Task-specific and stage-specific
configs are expected to be added in later phases.
